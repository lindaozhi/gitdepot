<h1><%= t('.list')%></h1>

<table>
  <tr>
  	<th><%= t('.user_name')%></th>
    <th><%= t('.order_name')%></th>
    <th><%= t('.address')%></th>
    <th><%= t('.order')%></th>
    <th><%= t('.paytype')%></th>
    <th><%= t('.order_status')%></th>
    <th></th>
    <th></th>
    <th></th>  
    <th></th> 
  </tr>
<% if @orders %>
<% @orders.each do |order| %>
  <% if @user.name == order.user_name && order.orderstatus == 0%>
  <tr>
  	<td><%= order.user_name%></td>
    <td><%= order.name %></td>
    <td><%= order.address %></td>
    <td><%= order.email %></td>
    <td><%= order.pay_type %></td>
   <!--  <td><% if order.orderstatus == 0%>
    		<%= 'unsent'%>
    	<% elsif order.orderstatus == 1 %>
    		<%= 'sent'%>
    	<% elsif order.orderstatus == 2 %>
    	    <%= 'cancel'%>
    	<% end %>
    </td> -->
    <td><%= 'unsent'%>
    <td><%= link_to 'Show', order %></td>
    <td><%= link_to 'Edit', edit_order_path(order) %></td>
     <td><%= link_to 'Cancel', {:controller => "orders",:action => "cancelorder",:id => order } %></td>
    <td><%= link_to 'Destroy', order, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
  <% end %>
<% end %>

</table>

<br />

<p><%= will_paginate @orders %></p>
<% end %>